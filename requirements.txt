# Core
torch>=2.1
torchvision>=0.16
numpy
pillow
tqdm
datasets

# Dataset download
requests          # streaming HTTP for the WildFake subset fetch
stream-unzip      # decode WildFake zips on the fly without a full download

# Model backbones (Person B)
open-clip-torch
timm

# Augmentations / image ops (Person A)
opencv-python
scikit-image
albumentations

# Eval / explainability (Person C)
scikit-learn
grad-cam
matplotlib
pandas

# Infra / misc (Person D)
pyyaml
